<?xml version="1.0" encoding="UTF-8"?>

<!--
//
// ===================================================
// FlashQ / HtmlQ Configuration file.
// ===================================================
//
-->

<config version="1.0" htmlParse="false">

  <!-- title of the study -->
  <item id="studyTitle">Social Support Sources among Chinese Students in the UK (Q-sort)</item>

  <!-- text-align property(left|right) -->
  <item id="textAlign">left</item>

  <!-- shuffle cards (true|false)  -->
  <item id="shuffleCards">true</item>

  <!-- login required (true|false) -->
  <item id="loginrequired">false</item>
  <!-- login with common password (leave blank if not required) -->
  <item id="loginPassword"></item>
  <!-- URL to individual login script (leave blank if not required) -->
  <item id="loginUrl"></item>
  <!-- request mode (post|get) -->
  <item id="loginUrlMethod"></item>

  <!-- activate/deactivate optional steps (true|false) -->
  <item id="showStep3">true</item>
  <item id="showStep4">true</item>
  <item id="showStep5">true</item>

  <!-- HtmlQ only: disable the in-window back button -->
  <item id="disableBackButton">true</item>

  <!-- define form elements for step5; only displayed if showStep5 is true -->
  <item id="form">

    <label>Year of birth*</label>
    <note>Please enter your year of birth (YYYY).</note>
    <input type="text" required="true" maxlength="4" restricted="0-9"></input>

    <label>Gender*</label>
    <note>Please select your gender.</note>
    <input type="radio" required="true">female;male;prefer not to say</input>

    <label>Current study level in the UK*</label>
    <note>Please choose the option that best describes your current status.</note>
    <input type="select" required="true">undergraduate;taught master;research master;PhD;language/pre-sessional;exchange/visiting;other</input>

    <label>Length of time in the UK*</label>
    <note>Please choose your approximate length of stay.</note>
    <input type="select" required="true">less than 3 months;3-6 months;7-12 months;1-2 years;more than 2 years</input>

    <label>Living arrangement*</label>
    <note>Which option best describes your current living situation?</note>
    <input type="select" required="true">living alone;living with roommates (shared accommodation);living with family/partner;university hall of residence;other</input>

    <label>Have you used university support services in the UK?</label>
    <note>This includes the international office, student services, counselling, etc.</note>
    <input type="radio" required="false">yes;no;not sure</input>

    <label>Comments (optional)</label>
    <note>If you would like to share anything about your sorting decisions, you can write it here.</note>
    <input type="textarea" required="false"></input>

    <note bg="false">Fields marked with an * are mandatory.</note>
  </item>

  <!-- URL for data transmission via POST/GET (leave blank if not required) -->
  <item id="submitUrl">exe.php?do=save</item>
  <!-- request mode (post|get) -->
  <item id="submitUrlMethod">post</item>

  <!-- data transmission via email (leave blank if not required) -->
  <item id="submitMail"></item>

</config>
